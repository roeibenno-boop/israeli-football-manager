# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local

# logs / misc build output
*.log
build/

# real club crest artwork (assets/crests/) -- user-supplied for local/
# internal-use-only running, not cleared for public redistribution. Kept on
# disk, deliberately excluded from git. See src/components/club-crest-images.ts
# for how the app still builds/runs without it (generated placeholder crests).
/assets/crests/

# local machine tooling (Node/Git installers, portable Node runtime) — not project source
/אפליציית כדורגל/

# typescript
*.tsbuildinfo

example

# generated native folders
/ios
/android
